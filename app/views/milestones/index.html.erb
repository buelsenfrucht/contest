<h1>Meine Meilensteine</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Datum</th>
      <th>Typ</th>
      <th>Wert</th>
      <th style="width: 90px; text-align: center;"></th>
    </tr>
  </thead>

  <tbody>
    <% @milestones.each do |milestone| %>
      <tr>
        <td><%= link_to l(milestone.created_at, :format => :short), edit_user_milestone_path(@user, milestone) %></td>
        <td><%= milestone.type %></td>
        <td><%= milestone.value %></td>
        <td style="text-align: center;">
          <%= link_to 'Löschen', user_milestone_path(@user, milestone), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Meilenstein anlegen', new_user_milestone_path(@user), class: 'btn btn-default btn-lg' %>
